# Schemas (outline)

TODO cover:
- Collection create request/response.
- Vector insert/upsert payloads (single and batch), delete payloads.
- Search request/response shapes (vector, text, range), filter structure, overrides.
- Embed request/response (single/batch) and usage metrics when available.
- Admin endpoints: index rebuild/status, compact, duplicates, config reload.
- Error envelope format and common codes.
